spring:
  datasource:
    url: jdbc:mysql://localhost:3306/project4systemDb
    username: 4system
    password: 4system
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  mvc:
    view:
      prefix: /templates/
      suffix: .html
  devtools:
    livereload:
      enabled: true
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
      
server:
  port: 8080